---
export interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<article
  class={`font-rubik mx-auto max-w-3xl py-4 text-base font-normal indent-4 ${className}`}
>
  <slot />
</article>
